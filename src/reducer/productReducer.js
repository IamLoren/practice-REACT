export const initialState = {
  productsData: [],
  skip: 0,
  loading: false,
  total: 0,
  isCartOpen: false,
  cartContent: [],
}

export const productReducer = (state, action) => {
  // // switch (key) {
  // //   case value:

  // //     break;

  //   default:
  //     return state;
  // }
}